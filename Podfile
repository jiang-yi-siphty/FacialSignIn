# Uncomment the next line to define a global platform for your project
platform :ios, '10.0'

target 'FacialSignIn' do
  # Comment the next line if you're not using Swift and don't want to use dynamic frameworks
  use_frameworks!

  pod 'Kingfisher'
  pod 'Alamofire'

  target 'FacialSignInTests' do
    inherit! :search_paths
    # Pods for testing
  end

  target 'FacialSignInUITests' do
    inherit! :search_paths
    # Pods for testing
  end

end
